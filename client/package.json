{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "serve -s dist -l $PORT"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "typescript": "~5.7.2",
    "vite": "^6.1.0"
  }
}
